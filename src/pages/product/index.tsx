// implementasi dynamic routing
// secara konsep, dynamic routing ini akan memungkinkan Next.js untuk menangkap parameter berikutnya pada url (misal id, slug, alias, dsb) dengan mendefinisikan nama file nya dengan format [id] atau [alias] atau [slug]

export default function ProductPage() {
    return (
        <>
            <h1>Product Page</h1>
        </>
    );
}