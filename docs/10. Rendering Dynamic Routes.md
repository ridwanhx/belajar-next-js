<!-- Eps. 10 - Rendering Dynamic Routes -->

<!-- Tahapan pembuatan halaman detail movie -->
1. inisialisasi tag Link dengan href tujuan ke halaman movie beserta mengirimkan id. contoh:  
src\views\Movie\index.tsx :
<!-- <Link href={`/movie/${movie.id}`} -->

 2. fetching data movie berdasarkan id yang dikirimkan menggunakan SWR (bisa di copas dari fetching movie sebelumnya, berlaku untuk Client-Side Rendering)
 src\pages\movie\[movie].tsx :
 <!-- 
    const { query } = useRouter();
    const { data, error, isLoading } = useSWR(`/api/movie/${query.movie}`, fetcher);

    return (
        {/* beri kondisi jika isLoading gagal / data tidak berhasil di load */}
        <MovieDetailViews movie={isLoading ? [] : data.data}/>
    );
  -->

  3. inisialisasi method fetching data berdasarkan id pada file service.ts
  src\lib\firebase\service.ts
  <!-- 
    // inisialisasi async function untuk fetch data berdasarkan id
    export async function retrieveDataById(collectionName: string, id: string) {

    // inisialisasi snapshot u/ ambil data berdasarkan id
    const snapshot = await getDoc(doc(firestore, collectionName, id));

    // inisialisasi data hasil snapshot
    const data = snapshot.data()

    // kembalikan nilai data
    return data;
   -->

  4. Rename file movie.ts menjadi [[...movie]].ts, lalu tambahkan kondisi jika request mengirimkan id melalui url
  src\pages\api\[[...movie]].ts :
  <!-- 
  // jika request mengirimkan id melalui url, maka
    if (req.query.movie![1]) {
        const data = await retrieveDataById("movies", req.query.movie![1]);
        res.status(200).json({ status: true, statusCode: 200, data });
    } else {
        // inisialisasi api integrasi firebase
        const data = await retrieveData("movies");
        res.status(200).json({ status: true, statusCode: 200, data });
    } -->

<!-- Untuk sisa implementasi menggunakan Server-Side dan Static Site Generation, bisa akses dokumentasi lengkapnya di direktori berikut : -->
src\pages\movie\[movie].tsx