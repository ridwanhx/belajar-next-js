<!-- Eps. 12 - Middleware -->

<!-- Middleware memungkinkan kita untuk menjalankan kode sebelum requestnya selesai dilakukan. -->

<!-- kita bisa membayangkan Middleware ini sebagai petugas penjaga gerbang tol ataupun misal petugas bea-cukai -->
<!-- jadi, intinya kita tidak diperbolehkan asal mengakses suatu halaman tertentu jika belum dapat izin masuk dari si middleware ini -->

<!-- aturan penting, kita harus menyimpan file middleware ini di dalam folder src/, atau istilahnya file middleware ini harus disimpan setara dengan folder pages/, lib/, styles/, types/, view/, dst.. (ataupun folder lainnya yang mengacu sebagai routing) -->
<!-- contoh struktur folder / folder tree :
src/
    components/
    lib/
    pages/
    views/
    ...
    middleware.ts
 -->

 <!-- direktori implementasi kode : -->
 <!-- src\middleware.ts -->

<!-- const config -->
 <!-- berisi matcher / daftar setiap routes/url yang akan diberlakukan middleware. -->
 <!-- setiap ada yang mengakses ke routes terdaftar pada config, akan secara otomatis memicu function middleware(), yang kalau berdasarkan studi kasus implementasi pada src/middleware.ts, setiap kali ada yang mencoba mengakses ke route/halaman url "/movie", "/product", dst.. Maka jalankan function middleware -->

<!-- function middleware() -->
<!-- memaksa setiap kali ada yang mencoba mengakses ke halaman terdaftar pada const config, akan dilempar kembali / redirect ke halaman / atau localhost:3000/ -->
<!-- setelah mengimplementasikan konsep autentikasi sederhanya, redirectnya diarahkan ke halaman /auth/login -->

<!-- Sebelum ini kita sudah mengimplementasikan konsep middleware, lebih tepatnya pada saat kita mencoba membuat useEffect() yang memungkinkan setiap kali ada yang mencoba akses ke halaman tertentu, akan langsung di redirect ke halaman tertentu -->
<!-- Nah, kalau begitu, jika kita bisa melakukan ini dengan useEffect, mengapa kita perlu melakukan ini semua di Middleware? -->
<!-- Ya, benar. seperti apa yang sudah kita implementasikan sebelumnya, memang mungkin jika kita mengimplementasikan konsep ini menggunakan middleware, hanya saja jika kita berkaca pada implementasi useEffect sebelumnya, kita mendefinisikan useEffectnya ini di tiap file page masing-masing halaman, sehingga yang terjadi adalah setiap kali kita mencoba mengakses ke halaman yang sudah dipasangi useEffect(), halaman tersebut tentunya akan dibaca dulu kode-nya oleh sistem (dari atas-bawah, kiri-kanan), dan yang terjadi kemudian adalah seolah ada efek glitch, dimana akan nampak oleh user bahwa halaman sudah mencoba diarahkan ke halaman yang dituju, namun kemudian baru di redirect setelah sepersekian milidetik (setelah sistem membaca ada baris useEffect), dan barulah redirect itu dijalankan -->
<!-- Sedangkan jika menggunakan middleware, bahkan sebelum mencoba mengakses ke halaman yang dituju, halaman sudah otomatis di-redirect ke halaman yang didefinisikan di function middleware() -->